import Menu from "./Menu";
import LoginInfo from './LoginInfo'

const Navbar = () => {
    return (
        <nav>
            <Menu />
            <LoginInfo />
        </nav>
    )
}

export default Navbar